# Python
__pycache__/
*.pyc

# Virtual environments
venv/
venv1/
venv2/
.venv/
.venv_revision/

# Data & checkpoints (large, regenerable/dataset artifacts)
data/
checkpoints/
nohup.out/

# Model weights kept out of git history
expert_0.pth
expert_1.pth
expert_2.pth
vit_b16_plant.pth

# Archives (duplicates of tracked source directories)
*.zip

# Local benchmark bundles and generated evaluation outputs
/pi_pytorch_cpu_runtime_bundle/
/context_moe/diagnostics/inference/
/context_moe/reports/**/run_*/

# Legacy/generated report exports; the authoritative source is
# /baocaorevisionmoe.tex and its frozen reviewer-evidence snapshot.
/Revision_context_moe_revision_full/reports/
/baocaorevisionmoe*.pdf

# Preserve the original Raspberry Pi evidence archive inside the reviewer
# snapshot even though other ZIP archives are ignored.
!Revision_context_moe_revision_full/reviewer_evidence/plantdoc_revision_results_20260920/run_04_pytorch_runtime_pi/pi_benchmark_results.zip

# Editor
.vscode/
